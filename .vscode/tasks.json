// Generate the task to auto build gradle when I save build.gradle
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build Gradle",
      "type": "shell",
      "command": "./gradlew build",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [
        "$gradle"
      ],
      "detail": "Generated task to build the project using Gradle."
    }
  ]
}